A fork of the source code for Steamry.

This version was updated to run on Cloudflare Workers and use D1 and Workers KV for data storage (instead of Postgres).

I revived it for some friends, so hopefully on the free tier is enough lol
